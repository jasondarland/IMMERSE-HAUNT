[project]
name = "immerse-designer"
version = "0.2.0"
description = "IMMERSE Designer unified desktop MVP"
readme = "README.md"
requires-python = ">=3.11"
dependencies = [
  "PySide6>=6.7",
]

[build-system]
requires = ["setuptools>=68", "wheel"]
build-backend = "setuptools.build_meta"
