[project]
name = "immerse-haunted-designer"
version = "0.1.0"
description = "IMMERSE Haunted Designer desktop MVP"
readme = "README.md"
requires-python = ">=3.11"
dependencies = [
  "PySide6>=6.7",
]

[build-system]
requires = ["setuptools>=68", "wheel"]
build-backend = "setuptools.build_meta"
